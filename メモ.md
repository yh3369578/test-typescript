
Controller呼び出しなど行う
DB接続もライブラリインストール（drizzle, prisma

フロントエンド（コンポーネントの勉強
標準搭載されているフック（コンポーネントとはべつもの）
コンポーネント内で使用する特殊な関数
useForm(ReactHookForm) 問い合わせフォームとか
useState(責務を分離して書く方法：useReducer)
useEffect
useRef
クライアントサイドコンポーネントで使用（フックはブラウザ内で使用する関数
storeという概念<-勉強（実現するためのライブラリ zustaund
components配下のディレクトリ構成も流派ある

## UI用ライブラリ 
https://ui.shadcn.com/

## Cloudflare の CLI
https://developers.cloudflare.com/workers/wrangler/ 

## 今回のプロジェクトの構成
Next.js  
TypeScript  


## 基礎知識
- ルーティング：App Router  
「next app router」で検索->ディレクトリ構成把握（app配下がapp routerで管理 ）

- layout.tsx (共通レイアウト)

- children に子コンポーネントが入る
```typescript
<body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>{children} </body>
```

- Next.js：defaultはSSR -> HTTP経由でAPI

- app直下にapiフォルダ作成  
コンポーネント(UI)は .tsx   
api は .ts  
